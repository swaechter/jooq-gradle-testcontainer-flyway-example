# Define an EditorConfig file to specify the most common editor configuration

# Most top configuration file
root = true

# Use Unix-style newlines + a trailing newline
[*]
end_of_line = lf
insert_final_newline = true

# Use 4 space indentation
indent_style = space
indent_size = 4

# Use UTF-8 as charset
charset = utf-8
